# LongX 0.3.1 Install-Only Delivery Manifest

Generated: 2026-07-21 22:47:08 +08:00
Source archive: not generated by this workflow.
Universal package: True
Package generators: ZIP

## Backend Results

| Backend | Status | Detail |
|---|---|---|
| UNIVERSAL | READY | Install package generated and verified. |

## Artifacts

- `LongX_Windows_CPU_NVIDIA_AMD_DCU_运行与测试说明_2026-07-18.docx` (10669 bytes)
- `LongX-0.3.1-UNIVERSAL-Windows-AMD64.zip` (75116 bytes)

## Recipient Usage

Unpack the archive, then point CMake to the install prefix:

```bash
cmake -S . -B build -DCMAKE_PREFIX_PATH=/path/to/LongX/install
cmake --build build
```

The package exposes `LongX::longx` through `find_package(LongX REQUIRED)`.

Minimal CMake consumer examples are included under:

```text
share/LongX/examples/install_consumer
```

The example directory includes `main.cpp`, `vector_add.cpp`, `stencil2d.cpp`, and `bench_vector_add.cpp`.
From that directory, configure it with this install prefix as `CMAKE_PREFIX_PATH` and run:

```powershell
ctest --test-dir build-serial -C Debug --output-on-failure
.\build-serial\Debug\vector_add.exe
.\build-serial\Debug\stencil2d.exe
.\build-serial\Debug\bench_vector_add.exe
```
